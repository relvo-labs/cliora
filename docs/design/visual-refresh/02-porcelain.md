# Porcelain｜明亮極簡 — 視覺風格規格

版本：0.1.0 · 狀態：Proposed · 日期：2026-09-09（UTC+08:00）
主題識別：`porcelain` · 適用產品：Cliora 遠端操作前端
共同要求：[共用規範](00-shared-foundation.md) · 決策入口：[索引](README.md)

## 1. 視覺定位

清楚、明亮、精準；以白色操作外框與深色終端形成明確工作焦點。

適合明亮環境與偏好淺色 UI 的使用者；深色終端需要獨立的文字與分頁語意色。

適用頁面包括 Session 工作台、Sessions、Nodes、概況，以及後續 Enrollment、Audit、Integrations。後三者是風格延伸規格，不表示目前原型已有這些頁面。

## 2. 色彩與語意 tokens

以下精確值對應 PR #52 原型的主題；它們是設計起點，不等於所有配對均已符合可讀性目標。

| Token | 原型值 | 使用位置 |
|---|---|---|
| `surface.canvas` | `#F9FAFC` | 整體畫布 |
| `surface.default` | `#FFFFFF` | 導覽與面板 |
| `surface.hover` | `#EEF2F7` | hover與浮層表面 |
| `terminal.background` | `#101416` | CLI與唯讀程式區 |
| `border.subtle` | `#E0E5EB` | 非必要裝飾分隔 |
| `text.primary` | `#25313C` | 一般介面文字 |
| `text.secondary` | `#77828D` | 輔助資訊 |
| `accent.primary` | `#286BF0` | 選取與主要動作 |
| `accent.subtle` | `#E9F0FF` | 低強度選取底 |

終端文字固定採 `#C9D3D8`；終端輸入 `#E4EEEB`。終端內的文字、游標與搜尋不得繼承淺色外框的深色文字。
正式實作另建立 `text.onAccent`、`text.onTerminal`、`border.control`、`focus.ring` 及 success/warning/error/info 的 foreground/background 配對；不得以 canvas 字色推導所有按鈕文字。

### 對比檢查（sRGB、不透明色值）

> **修訂（`plan/28`，2026-09-08，ADR 0027）。** 本表原本只驗**三組**配對，
> 而且第三列（「原型主按鈕文字／accent.primary」）是**拿 `surface.canvas` 當按鈕文字**
> 算出來的——同一頁下面就寫著「不得以 canvas 字色推導所有按鈕文字」。
> 兩處都在這裡修正：第三列改為明確的 `text.onAccent` ／ `accent.strong`，
> 並補上原本漏檢的導覽／分頁選中態、控制邊界、焦點環與終端四類配對。

| 配對 | 值 | 實測 | 目標 | 判定 |
|---|---|---:|---|---|
| `text.primary` ／ `surface.default` | `#25313C` / `#FFFFFF` | 13.26:1 | 4.5 | 通過 |
| `text.secondary` ／ `surface.default` | `#5E6B78` / `#FFFFFF` | 5.45:1 | 4.5 | 通過 |
| `text.onAccent` ／ `accent.strong`（主按鈕） | `#FFFFFF` / `#1B54C4` | 6.75:1 | 4.5 | 通過 |
| `accent.primary` ／ `accent.subtle` | `#286BF0` / `#E9F0FF` | 4.13:1 | 4.5 | **不通過** |
| `accent.strong` ／ `accent.subtle`（導覽／分頁選中） | `#1B54C4` / `#E9F0FF` | 5.90:1 | 4.5 | 通過 |
| `border.control` ／ `surface.default` | `#838689` / `#FFFFFF` | 3.66:1 | 3.0 | 通過 |
| `focus.ring` ／ `surface.default` | `#286BF0` / `#FFFFFF` | 4.72:1 | 3.0 | 通過 |
| `focus.ring` ／ `terminal.background` | `#286BF0` / `#101416` | 3.93:1 | 3.0 | 通過 |
| `text.onTerminal` ／ `terminal.background` | `#C9D3D8` / `#101416` | 12.17:1 | 4.5 | 通過 |

**第 4 列是本文件原本漏檢的配對，而它不通過。** `accent.primary` （`#286BF0`）疊在 `accent.subtle`（`#E9F0FF`）上實測 **4.13:1**，而那正是「導覽與分頁選中態」要用的那一個配對。
處置：新增 **`accent.strong` = `#1B54C4`**（實測 5.90:1），選中態文字與主按鈕底一律用它；`accent.primary` 保留為**不承載文字**的填色（選中底線、進度、導覽項的 2px 左邊線）。

完整的配對清單（每個元件宣告它用到的配對）與五款主題 195 組的實測結果在
[`plan/28/02-token-contract-and-themes.md`](../../../plan/28/02-token-contract-and-themes.md) §6，
由 `frontend/src/theme/theme.contrast.test.ts` 逐組斷言。
狀態三元組、ANSI 十六色與半透明疊加另行驗證（半透明由瀏覽器實測，算術檢查算不了）。
token變更寫入正式實作的主題表，不直接改既有產品的全域樣式。

## 3. 字體、密度與空間

系統無襯線字體；標題 26px／600，正文 14px／400，操作 14px／500，輔助資訊 12px。避免過細字重。

- 終端預設14px，**行高 1.2**，使用者可調12–20px；**修訂（`plan/28`，chromium 實測）：原本寫行高 1.6，而 `14px/1.6` 在 1440×900 只給 26 列，跌破 `plan/09` 的 ≥ 30 列閘門（`14px/1.2` 是 35 列）。理由與完整量測見 `01-graphite.md` §3 同一行。**字體採本機等寬字體，新增字型需隨應用打包。
- UI間距以4px為基本單位：4／8／12／16／20／24／32。
- 表格列48px，表單控制項36px；觸控目標至少44×44px。
- 圓角基準8px；陰影只用於真正浮在內容上的元件。
- 文字縮放至200%時控制項仍可操作，長Session名稱與路徑不得遮住必要動作。

## 4. 桌面版面

文字導覽 216px；檔案欄 258px。頂部麵包屑 56px；工作資訊標頭 80px；分頁 44px；狀態列 28px。工作面外圍留白 16px，深色終端只保留一層容器。

上述尺寸是此風格的新提案，不是對原型現有尺寸的描述。只由App Shell擁有視窗高度，工作面使用minmax(0, 1fr)及面板內滾動。狀態提示出現時壓縮面板可用高度，不讓整頁產生第二條滾動軸。

終端應取得中央可用面積；CLI、系統終端與預覽分頁一次只顯示一個主面板。不要為看起來像IDE而強制加入多欄分割終端。

## 5. 元件規則

| 元件 | 規則 |
|---|---|
| 導覽與分頁 | 鈷藍文字＋淡藍底＋2px側邊標記；分頁白底，使用藍色底線，不將活動分頁改成黑底黑字。 |
| 表面、卡片 | 白色面板與冷灰背景建立層次；表格使用細線，不使用斑馬紋；浮層可用 0 8px 28px #25313C18。 |
| Primary button | 同一區域只保留一個最高強度動作；使用text.onAccent配對 |
| Secondary／Ghost | 中性底與必要邊界；hover提升背景，不改尺寸 |
| Focus | 2px清楚外框、2px offset；不以hover代替鍵盤焦點 |
| Disabled | 保留標籤；以文字及不可操作語意表示，避免單靠透明度 |
| 表單 | label常駐、錯誤在欄位下方；必填與驗證結果可讀 |
| 狀態標籤 | 成功／離線／重連／結束均帶文字；與runtime標籤分開 |
| 危險操作 | 終止放操作選單並確認Session名稱；不占據日常主動作位置 |
| 表格 | 主要名稱可開啟；路徑為次列；空清單與篩選無結果使用不同文案 |
| 檔案樹 | 展開箭頭、folder/file icon和檔名；選取同時改底色與字重 |
| Toast | 成功短暫提示；失敗保留於相關區域並提供重試 |
| Dialog | 焦點受限於dialog、Esc關閉可取消流程、關閉後返回觸發點 |

## 6. 頁面延伸

- **工作台**：工作標頭顯示Session與Node身份；狀態列分開顯示Session狀態、瀏覽器連線與控制權。
- **Sessions**：名稱與路徑為主欄，Node／Runtime／狀態是比較欄；建立後開啟新工作台。
- **Nodes**：識別、上線狀態、Runtime可用性為第一層；資源資訊不足顯示「無資料」，不用0代替。
- **概況**：繼續工作在指標前；統計顯示回報時間與完整性，不加入不存在的即時監測。
- **管理頁**：Enrollment採表單，Audit採可篩選表格，Integrations採設定群組；沿用本風格元件，不另外建立一套卡片語彙。

## 7. 響應式與動態

- ≥1440px：使用本文件桌面配置。
- 1024–1439px：縮減外距並收合導覽；右側欄保留收合入口。
- 768–1023px：檔案欄改覆蓋抽屜；不將功能直接隱藏。
- <768px：主導覽改可開啟選單，主面板單欄；工作路徑可展開查看；終端輸入跟隨現有授權，不因裝置尺寸自行變更權限。
- 切換狀態120–180ms，縮減動態偏好下停用非必要動畫。
- 終端內容不做淡入、縮放或打字動畫；重連時保留可讀輸出與明確連線狀態。

## 8. 原型差距與實作邊界

原型活動分頁使用深色 terminal 背景與淺色主題文字，正式整合前必須修正對比；次文字 #77828D 在白底不適合直接用於小字。

原型所有畫面採合成資料，無真實終端或遠端授權。正式整合保留既有Vue、Pinia、xterm.js、Monaco與協定（**Naive UI 已於 ADR 0027 §6 移除相依**：它從未被使用——零個元件、一個型別 import、而持有它的檔案沒有被任何檔案 import，所以「保留」它實際上是「導入」它）；視覺變更不得使切換面板卸載仍使用中的終端。
圖稿中的裝飾、區域或延遲資訊不得被當成已有API欄位。

## 9. 禁用與驗收

避免多層白卡陰影、淺灰文字過淡，以及直接套用深色主題按鈕文字色。

- [ ] 所有使用者需要閱讀的文字配對達到共用可讀性目標，包含終端及狀態標籤。
- [ ] 選中、hover、focus、disabled與error外觀可辨識。
- [ ] 1440×900、1024×768、390×844下核心操作可觸及，無不必要整頁橫向溢出。
- [ ] 長名稱、長路徑、空資料、離線、重連、唯讀、終止及上傳失敗均有畫面。
- [ ] 主題切換保留Session、終端捲動位置、控制權與未送出輸入。
- [ ] 所有量測結果在實作PR中補上；本文件不宣稱已完成瀏覽器驗收。
