{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Daemon -> Central: the tunnel is gone, and why. `reason` is a closed vocabulary so an operator can tell an explicit close from a TTL expiry from the daemon giving up on reconnects.",
  "type": "object", "additionalProperties": false,
  "required": ["tunnel_id", "reason"],
  "properties": {
    "tunnel_id": {"type": "string", "format": "uuid"},
    "reason": {"enum": ["requested", "expired", "provider_failed", "shutdown"]}
  }
}
