.venv/
node_modules/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.pyc
daemon/bin/
frontend/dist/
backend/dist/
frontend/coverage/
frontend/playwright-report/
frontend/test-results/
frontend/tsconfig.tsbuildinfo
# Merge/patch leftovers.
*.orig
# Locally generated evidence packs / perf runs (CI uploads its own per-run dirs).
artifacts/*/local/
# Filled-in deployment env. `deploy/compose/.env.example` is the committed template;
# a real `.env` holds CLIORA_JWT_SECRET, CLIORA_TOKEN_PEPPER and the database password.
# Never committed — `.env.example` does not match this pattern and stays tracked.
.env
