# 07 — 實作進度與證據

本檔隨實作更新；「證據」欄只填**實際跑過的指令與其輸出位置**，不填計畫中的測試。

最後更新 2026-08-02：**計畫完成，`WE-01`–`WE-12` 全部未開始。**

> ### 下一輪從這裡接手
>
> **第一件事是 `WE-01` 的第 1 與第 6 項**（`08-…md`）。兩者都是閘門，
> 都只需要一支小程式或三行 shell，而且任何一個不成立都會改變設計而不只是參數：
>
> - 第 1 項（`os.Root.Link` 在目的地已存在時是否 `EEXIST`、
>   `os.Root.Rename` 能否搬動整棵目錄、節點的檔案系統是否支援 hardlink）
>   決定回收桶（`00-…md` D6，**檔案與目錄兩半各有一個前提**）與
>   更名的不覆寫保證（`02-…md` §4.2）能不能照計畫做。
> - 第 6 項（git worktree 的 `.git` 是檔案）決定 `FR-FILE-011.AC-06` 的寫法。
>   **這一項今天是一個實際存在的缺口**：那個檔案現在就可以被預覽，
>   而本期會讓它可以被編輯。
>
> **第二件事是 `WE-02a`。** 閘門仍然存在：ADR 0025 核准之前不得動 daemon 程式碼。
> `WE-02a` 沒有前置（W1–W4 對編輯的逐條落地、D1 的線、D4 的對稱性都不依賴實測），
> 所以它可以與 `WE-01` 並行，而且應該最先寫完 —— `WE-03`、`WE-04` 與
> `WE-10` 都靠它。
>
> **本期的硬性順序**：刪除不得早於回收桶；**刪除資料夾**還要等 `WE-03b` 的走訪
> （`00-…md` D21、D22、`06-…md` §6）。
>
> **從 p13 帶過來的一筆待辦：** `docs/security-review-p13.md` 第 8 題
> 把「W1–W4 是否真的可以承接下一條寫入路徑」列為待答，並指出
> **W2 對編輯只成立一半**、以及「敏感檔案政策要套到寫入方向」與
> 「`.git/` 要保護」兩件事當時完全不存在。本期的回答分別在
> `01-…md` §1.2（W2 的第三腿＝回收桶）與 `00-…md` D4（同一個函式，兩個方向）。
> **收尾時要回頭把 p13 那一題標記為已回答**，否則它會被讀成仍然開著。
>
> **另一筆從 p13 帶過來的東西，性質不同：** `plan/13/05-…md` §2.2 計畫在檔案樹的
> `.cliora/` 旁加一個「平台寫入」標記，但它沒有被實作，而 `WF-08` 被記為完成
> （已核對：`frontend/src/components/file/` 底下沒有相關字串）。
> 本期要補上（`05-…md` §3.2），並在 `plan/13/07-…md` 的差異表補一列 ——
> 一個被記成完成卻沒有落地的項目，留著會讓下一份狀態表不可信。

## `WE-01` 實測結果

見 [`08-open-measurements.md`](08-open-measurements.md)。**六項全部未測。**

| # | 量測 | 閘門 | 狀態 |
|---|---|---|---|
| 1 | `os.Root.Link`／`Rename` 的語意與檔案系統支援（含整棵目錄的搬移） | ✔ | 未測 |
| 2 | Monaco 0.56 的往返忠實度 | | 未測 |
| 3 | rename-over 對 watcher／CLI／hardlink 的影響、412 的實際頻率 | | 未測 |
| 4 | 敏感規則在寫入方向的誤擋率 | | 未測 |
| 5 | SHA-256 對 2 MiB 的成本、目錄走訪對 20000 項的成本 | | 未測 |
| 6 | git worktree 的 `.git` 是檔案 | ✔ | 未測 |

## Ticket 進度

| Ticket | 狀態 | 證據 |
|---|---|---|
| `WE-01` 行為實測 | 未開始 | — |
| `WE-02a` 姿態（ADR 0025 §1–§3、ADR 0024 加註、ADR 0015 amendment） | 未開始 | — |
| `WE-02b` 需求（`FR-FILE-010`／`011`、四處 PRD 修訂、tech §11.10） | 未開始 | — |
| `WE-03` daemon 寫入面（四個動詞） | 未開始 | — |
| `WE-03b` 目錄的有界子樹走訪 | 未開始 | — |
| `WE-04` 回收桶、配額、清理、設定、`doctor` | 未開始 | — |
| `WE-05` 契約 v1.9.0 | 未開始 | — |
| `WE-06` Central 端點、RBAC、稽核、migration | 未開始 | — |
| `WE-07` 前端編輯模式 | 未開始 | — |
| `WE-08` 前端檔案樹四個動詞（含資料夾） | 未開始 | — |
| `WE-09` 唯讀敘事與能力旗標 | 未開始 | — |
| `WE-10` `.agent/skills` 五份修訂 | 未開始 | — |
| `WE-11` evidence、三個 gate、runbook、release note | 未開始 | — |
| `WE-12` 安全審查、exit gate | 未開始 | — |

## 與計畫的差異

（實作開始後填寫。體例沿用 `plan/13/07-implementation-status.md`：
每一列是「計畫怎麼說 ／ 實際怎麼做 ／ 為什麼」，
而「為什麼」那一欄要寫出是被什麼抓到的 —— 測試、審查，還是使用者。）

| # | 計畫怎麼說 | 實際怎麼做 | 為什麼 |
|---|---|---|---|
| — | — | — | — |

## 環境事實（沿用 p13 實測，未重新確認）

| 事實 | 值 |
|---|---|
| 工具鏈不在預設 PATH 上 | `export PATH="$HOME/.local/bin:/usr/local/go/bin:$HOME/.nvm/versions/node/v22.23.2/bin:$PATH"` |
| Go | 1.26.5（`.go-version`、`daemon/go.mod` 皆為 1.26） |
| Monaco | `monaco-editor ^0.56.0`（`frontend/package.json`） |
| Postgres | `postgresql+asyncpg://cliora:cliora@127.0.0.1:5432/cliora_test`，已 `alembic upgrade head` 到 `0019` |
| `make test-db` 要同時設兩個變數 | `CLIORA_TEST_DATABASE_URL` **與** `CLIORA_DATABASE_URL` |
| 瀏覽器 e2e 需要有 online node 的 stack | `scripts/e2e/run-stack.sh`；p13 在本機記為 skip |
